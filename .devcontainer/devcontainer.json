{
  "name": "whonb-nbcode",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "features": {
    // "ghcr.io/devcontainers/features/git:1": {},
    // "ghcr.io/devcontainers/features/github-cli:1": {}
  },
  "remoteUser": "node",
  "customizations": {
    "vscode": {
      "settings": {
        "terminal.integrated.defaultProfile.linux": "bash",
        "dev.containers.copyGitConfig": false // 禁用从宿主机拷贝 git 配置
      }
    }
  },
  // "postCreateCommand": "node -v && git --version && gh --version && gemini --version"
  "mounts": [
    "source=${localWorkspaceFolder}/.devcontainer/home,target=/home/node,type=bind,consistency=cached"
  ]
}